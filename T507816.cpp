#include <bits/stdc++.h>
using namespace std;

int n, q;
int l[1000001], r[1000001];
int x;

int main() {

    cin >> n >> q;

    for(int i = 1; i <= n; ++i) {
        cin >> l[i] >> r[i];
    }
    while(q--) {
        cin >> x;

    }
    return 0;
}