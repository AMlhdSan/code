#include <bits/stdc++.h>
using namespace std;

int n, c;
int sum[1001];
int maxx;

int main() {

    ios::sync_with_stdio(false);

    cin >> n >> c;

    for(int i = 1; i <= n; ++i) {
        cin >> a[i];
    }

    cout << maxx << endl;
    return 0;
}