#include <bits/stdc++.h>

#define N 100001

using namespace std;

int n;
int dp[N]; // dp[i] : 第i位均为1的最长子序列长度
int a[N];

int main() {

    cin >> n;

    for(int i = 1; i <= n; ++i) {
        cin >> a[i];

    }



    return 0;
}