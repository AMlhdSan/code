#include <bits/stdc++.h>

#define N 100001

using namespace std;

int n, m;
int a[N];
int opt;
int ll, rr, kk, dd, pp;

int main() {

    cin >> n >> m;

    for(int i = 1; i <= n; ++i) {
        cin >> a[i];
    }

    while(m--) {
        cin >> opt;
        if(opt == 1) {
            cin >> ll >> rr >> kk >> dd;
        }
        else {
            cin >> pp;
            cout << 
        }
    }

    return 0;
}