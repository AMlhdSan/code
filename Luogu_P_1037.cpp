#include <bits/stdc++.h>
using namespace std;

string n;
int k;
int x[N], y[N];

int main() {

    cin >> n + 1;

    cin >> k;

    for(int i = 1; i <= k; ++i) {
        cin >> x[i] >> y[i];
    }

    for(int i = 1; i <= n; ++i) {
        
    }

    return 0;
}