#include <bits/stdc++.h>
using namespace std;

int n, k;
int x[501];
int y[501];

int main() {

    cin >> n >> k;
    for(int i = 1; i <= n; ++i) {
        cin >> x[i] >> y[i];
    }
    
    return 0;
}