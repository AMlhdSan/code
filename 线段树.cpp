#include <iostream>
using namespace std;

int n, m;
int a[100001];
int op;
int x, y, k;

int main() {

    cin >> n >> m;

    for(int i = 1; i <= n; ++i) {
        cin >> a[i];
    }

    while(m--) {
        cin >> op;
        if(op == 1) {
            cin >> x >> y >> k;
        }
        else {

        }
    }
    return 0;
}