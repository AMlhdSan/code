#include <bits/stdc++.h>

#define N 101

using namespace std;

int d, g;
int t[N], f[N], h[N];

int main() {

    cin >> d >> g;


    for(int i = 1; i <= g; ++i) {
        cin >> t[i] >> f[i] >> h[i];
    }

    

    return 0;
}