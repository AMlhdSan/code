#include <bits/stdc++.h>

#define N 101

using namespace std;

int n, s;
int a[N];
int c[N];
int dp[N][N];

int main() {

    ios::sync_with_stdio(false);

    cin >> n >> s;

    for(int i = 1; i <= n; ++i) {
        cin >> a[i] >> c[i];
    }

    
    return 0;
}