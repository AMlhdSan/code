#include <bits/stdc++.h>

#define M 401

using namespace std;

int n, m;
int x[M], y[M];

int main() {

    cin >> n >> m;

    for(int i = 1; i <= m; ++i) {
        cin >> x[i] >> y[i];
    }

    

    return 0;
}