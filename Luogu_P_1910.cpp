#include <bits/stdc++.h>

#define N 101

using namespace std;

int n, m, x;

int a[N], b[N], c[N];

int dp[N];

int main() {

    cin >> n >> m >> x;

    for(int i = 1; i <= n; ++i) {
        cin >> a[i] >> b[i] >> c[i];
    }

    return 0;
}