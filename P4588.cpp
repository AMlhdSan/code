#include <bits/stdc++.h>
#define N 100001
#define ll long long
#define ls (p << 1)
#define rs (p << 1 | 1)
using namespace std;

int T;
int q, mod;
ll tree[N << 1];

void build(int p, int l, int r) {

}

int main() {

    cin >> T;
    while(T--) {
        cin >> q >> mod;

        build(1, 1, q);

        while(q--) {

        }
    }

    return 0;
}